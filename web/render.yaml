services:
  - type: web
    name: walle-biblioteca
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn walle_biblioteca.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
